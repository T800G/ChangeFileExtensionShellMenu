This a simplified Visual Basic Script version that every (non-administrator) user can set up manually.

Copy chx.ico and chx.vbs to a suitable folder (eg. C:Users\T800\AppData\Local).
Edit paths inside the install.reg file, then double-click it to add entries into the registry.

To remove, double-click remove.reg file to delete registry entries, then delete all files.